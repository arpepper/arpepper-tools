#!/bin/tcsh -f

set histchars

#setenv PATH `/bin/showpath /u/arpepper/bin standard`

set TMPDIR = /tmp/arpepper-mail

if ( ! -d $TMPDIR/. ) then
	mkdir $TMPDIR
endif
setogm -m 700 $TMPDIR
echo $TMPDIR

